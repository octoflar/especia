language: cpp
addons:
  apt:
    packages:
      - liblapack-dev
script:
  - mkdir cmake-build-release
  - cd cmake-build-release
  - cmake -DCMAKE_BUILD_TYPE=Release ..
  - make -j clean all util
  - make "$TEST"
matrix:
  include:
    - os: osx
      env:
        - TEST="unittests"
    - os: osx
      env:
        - TEST="systemtests"
    - os: linux
      env:
        - TEST="unittests"
      dist: trusty
      sudo: required
    - os: linux
      env:
        - TEST="systemtests"
      dist: trusty
      sudo: required
